#include <stdio.h>
#include <math.h>


void func();
int main() {
    
    int x_domain[3] ={-2, 2, 500};
    int y_domain[3] ={-2, 2, 500};
    int bound = 2;
    int max_iteractions = 1000;

    int iteraction_array[]= {};

    for(int i = 0; i < y_domain[2]; i++){
        int row[] = {};
        for(int j = 0; j < x_domain[2]; j++){
            func();
            int z = 0;
            int p = 2;
            int c = complex(x)
        }
    }
    return 0;
}

void func(){
    int lambda = 0;
    int z = 0;
    int p = 0;
    int c = pow(z, p) + c;
}